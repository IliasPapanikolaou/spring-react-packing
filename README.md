# Packaging Springboot with Frontend (React)

POST: localhost:8080/clients
```json
{
  "name": "John Rambo", 
  "email": "john.rambo@mail.com"
}
```

Application build:

mvn clean package

Application url: 

http://localhost:8080